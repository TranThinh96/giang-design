/**
 * Intentionally empty: the sibling layout renders the entire Keystatic app and
 * ignores `children`. This file exists only so the route matches `/keystatic`
 * and everything under it.
 */
export default function KeystaticPage() {
  return null;
}
