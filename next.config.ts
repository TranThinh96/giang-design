import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    // Portfolio/product photography is not yet supplied by the client — see
    // PLAN.md §9.1. Real domains get added here once assets land.
    remotePatterns: [],
  },
};

export default nextConfig;
